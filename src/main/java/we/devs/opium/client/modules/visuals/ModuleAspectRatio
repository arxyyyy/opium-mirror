package we.devs.opium.client.modules.visuals;

import we.devs.opium.api.manager.module.Module;
import we.devs.opium.api.manager.module.RegisterModule;

@RegisterModule(name = "AspectRatio", description = "Have stretched res like fortnite kids", tag = "AspectRatio", category = we.devs.opium.api.manager.module.Module.Category.VISUALS)
public class ModuleAspectRatio extends Module {
    
    public static we.devs.opium.client.modules.visuals INSTANCE = new we.devs.opium.client.modules.visuals.ModuleAspectRatio();
    public Setting ratio = new Setting("Ratio", 1.78f, 0.1f, 5f);

    private ModuleAspectRatio() {
    }

    public static ModuleAspectRatio getInstance() {
        return new ModuleAspectRatio();
    }
}
